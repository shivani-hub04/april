#include<stdio.h>
int main()
{
	int n,i;
	printf("enter the entries");
	scanf("%d",&n);
	int a[n];
	for(i=0;i<n;i++)
	{
		printf("\n enter the number:");
			scanf("%d",&a[i]);
		}
	int s=0;
	for(i=0;i<n;i++)
	{
		s+=a[i];
	} 
	printf("sum:%d",s);
	return 0;
}
